suppose they two pointer meet at K
pointer 1x path: X+nY +K
pointer 2x path: X+mY+K

then: X+K = (m-2*n)Y

it means that 1x will go X far would reach the cycle point

So let two pointers p1 & p2 start from head and K seperately, they would meet at the cycle start position
