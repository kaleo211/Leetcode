The problem allow us to use constant space.
We all know that most sort algorithms have nlogn time complexity base on random access datastruct like array.
So just use a ArrayList to store each node's address to have this ability.
Xuebin
